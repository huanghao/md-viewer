# case-23: 搜索打开新文件后正文回到顶部

## 目标

- 当内容区已经滚动后，通过搜索打开另一个文件，正文滚动位置应重置到顶部。

## 步骤

1. 先打开一个较长文件并将 `#content` 向下滚动。
2. 在搜索框输入关键词并按 Enter，打开另一个文件。
3. 检查新文件已成为当前项，且 `#content.scrollTop` 回到 0。

## 预期

- 切换到新文件后不会沿用旧文件滚动位置。
