# case-22: 同步弹层关闭能力回归

## 目标

- 同步弹层右上角 `×` 可见且可点击关闭。
- 点击弹层外（遮罩空白区）可关闭弹层。

## 步骤

1. 打开测试文件并点击工具栏同步按钮。
2. 校验 `#syncDialogOverlay` 进入显示状态且 `.sync-dialog-close` 可见。
3. 点击 `.sync-dialog-close`，弹层关闭。
4. 再次打开同步弹层，点击遮罩空白区，弹层再次关闭。

## 预期

- 两种关闭路径都能稳定关闭弹层，不影响后续再次打开。
