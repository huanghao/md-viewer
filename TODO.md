# 待完成功能

这个文档记录待完成的任务。
1）我会不定期往后追加任务
2）Agent可以每次完成其中一个，完成后在这条任务的下方标记完成状态、时间、简要描述、和对应的commit
3）Agent在完成任务的过程中，遇到需要和我确认的，请编写设计文档，等待我的确认后再继续

## cli还是默认切换，
- cli默认切换，用参数控制不切换

**已完成** - 2026-02-27
- 修改CLI默认行为：`focus` 默认为 `true`
- 新增 `-n, --no-focus` 参数用于控制不切换
- 帮助文档和示例已同步更新

## 正在打开的文件效果
- 当前正在渲染的文件，在左侧列表上的样式要稍微变化一下。你先设计一下，给我几个方案，我选择，先别着急开发。不要太突出了，从其他一些编辑器之类的工作里找类似的效果。

**已完成** - 2026-02-27
- 选择方案 A：左侧 3px 蓝色竖线指示器（VS Code 风格）
- 修改 `css.ts`：添加 `.file-item.current` 样式
- 修改 `app.ts`：`renderFiles()` 函数根据 `state.currentFile` 添加 `current` 类

```
┌─────────────────┐
│ 📄 README.md    │  ← 在标签栏打开，但不是当前文件
│ 📄 TODO.md    │ │  ← 当前正在渲染的文件，左侧有蓝色竖线
│ 📄 guide.md     │
└─────────────────┘
```

## 实现config
- 服务端和客户端都从 ~/.config/md-viewer/config.json里读配置
- 整理现在需要放到配置里的配置项
- 写配置、和配置项参数的策略解释，放到docs里

**已完成** - 2026-02-27
- 创建 `src/config.ts`：配置加载、默认值、环境变量覆盖
- 更新 `server.ts`：使用配置系统初始化端口和主机
- 更新 `cli.ts`：使用配置系统的默认值，显示配置路径
- 创建 `docs/config-strategy.md`：配置策略说明文档

## 生成man手册
- cli：`man/md-viewer.1`
- config: `man/md-viewer.config.5`
- 服务端需要man吗？惯例是什么？先给我解释一下，再做开发
- 在项目的AGENTS.md里添加一个约定：更新了cli的参数以后要更新man手册，更新了配置相关代码也要更新man和配置说明文档

**已完成** - 2026-02-27
- 创建 `man/md-viewer.1`：CLI 和 Server 使用手册（方案 A：合并 CLI 和 Server）
- 创建 `man/md-viewer.config.5`：配置文件格式手册
- 创建 `AGENTS.md`：添加 man 手册维护约定

## 文件更新提示
小红点，或者其他工具类似的视觉效果，你告诉我，我判断一下

## tab太多怎么办
同样需要给我一些其他工具的设计方案，我来选择

# -------- 以下的任务不要实现，我再想想 -------


## 同步km-cli

## 设计一个在本地快速打开的动作

## 左侧列表交互优化


## 启动playwright进行手工测试
同时让agent记录测试过程，发现错误自动修复
监控动作看看和它实现的预期是否一致

kimi说不太现实


## 页面上输入文件路径的地方能不能给些输入提示
你想想其他工具在输入文件的控件上都是怎么实现的？给几个方案让我选