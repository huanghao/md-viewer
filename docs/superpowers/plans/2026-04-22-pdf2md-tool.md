# pdf2md 工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `tools/pdf2md/pdf2md.py`，把学术 PDF 转换为结构化 Markdown（摘要+正文+参考文献）并可选生成翻译 sidecar，输出到以 PDF 文件名命名的目录。

**Architecture:** 单文件 Python 脚本，内部按职责拆分为独立函数模块：markitdown 转换 → 论文分段拆分 → TOC 提取 → 标题插入 → 翻译 sidecar。每个步骤函数独立可测，主入口 `main()` 串联所有步骤。

**Tech Stack:** Python 3.12, markitdown[pdf], pypdf, anthropic SDK, python-dotenv, pytest

---

## File Map

| 文件 | 操作 | 说明 |
|------|------|------|
| `tools/pdf2md/pdf2md.py` | Create | 主脚本，所有逻辑 |
| `tools/pdf2md/requirements.txt` | Create | Python 依赖 |
| `tools/pdf2md/.python-version` | Create | Python 版本声明 |
| `tools/pdf2md/README.md` | Create | 使用说明 |
| `tools/pdf2md/tests/test_split.py` | Create | 论文拆分逻辑单元测试 |
| `tools/pdf2md/tests/test_toc.py` | Create | TOC 提取单元测试 |
| `tools/pdf2md/tests/test_structure.py` | Create | 标题插入单元测试 |

---

## Task 1: 项目脚手架

**Files:**
- Create: `tools/pdf2md/requirements.txt`
- Create: `tools/pdf2md/.python-version`
- Create: `tools/pdf2md/README.md`
- Create: `tools/pdf2md/pdf2md.py`（骨架）

- [ ] **Step 1: 创建目录和配置文件**

```bash
mkdir -p tools/pdf2md/tests
```

创建 `tools/pdf2md/.python-version`：
```
3.12
```

创建 `tools/pdf2md/requirements.txt`：
```
markitdown[pdf]>=0.1.0
pypdf>=4.0.0
anthropic>=0.49.0
python-dotenv>=1.0.0
pytest>=8.0.0
```

- [ ] **Step 2: 创建 `tools/pdf2md/pdf2md.py` 骨架**

```python
#!/usr/bin/env python3
"""
pdf2md.py — 学术 PDF → 结构化 Markdown

用法:
  python pdf2md.py paper.pdf
  python pdf2md.py paper.pdf --translate
  python pdf2md.py paper.pdf --translate-only
  python pdf2md.py paper.pdf --model claude-opus-4-7
"""
from __future__ import annotations

import argparse
import json
import re
import sys
import time
import urllib.error
import urllib.request
from datetime import datetime, timezone
from pathlib import Path


# ---------------------------------------------------------------------------
# 论文拆分
# ---------------------------------------------------------------------------

def split_paper(raw_text: str) -> dict:
    """
    把 markitdown 输出的原始 MD 文本拆分为各部分。

    返回:
        {
            "meta_lines": [...],   # Abstract 之前的行
            "abstract": str,       # Abstract 正文（不含 Abstract 标题行）
            "body": str,           # 正文（第一章到 References 之前）
            "references": str,     # References 之后，附录之前
            "appendix": str,       # Contributors/Acknowledgements/Appendix 等
        }
    """
    pass  # Task 2 实现


def parse_meta(meta_lines: list[str]) -> dict:
    """
    从 arxiv 元信息行解析结构化 JSON。

    返回:
        {
            "title": str | None,
            "authors": list[str],
            "date": str | None,      # YYYY-MM-DD
            "arxiv_id": str | None,
            "url": str | None,
        }
    """
    pass  # Task 2 实现


# ---------------------------------------------------------------------------
# TOC 提取
# ---------------------------------------------------------------------------

def extract_toc(pdf_path: Path) -> list[dict]:
    """
    用 pypdf 读取 PDF outline，返回扁平化带编号的列表。

    返回:
        [{"num": "3.1", "title": "Pre-Training Data", "level": 2, "pageNum": 4}, ...]
    """
    pass  # Task 3 实现


# ---------------------------------------------------------------------------
# 标题插入
# ---------------------------------------------------------------------------

def insert_headings(body: str, toc: list[dict], audit_path: Path) -> str:
    """
    在 body 文本中按 TOC 插入 Markdown 标题。
    策略 A: 精确匹配；策略 B: 模糊匹配；策略 C: 跳过并记录 audit。

    返回插入标题后的 body 文本。
    """
    pass  # Task 4 实现


# ---------------------------------------------------------------------------
# 翻译 sidecar
# ---------------------------------------------------------------------------

TRANSLATE_API = "http://localhost:3000/api/translate"


def translate_text(text: str) -> str:
    """调用 mdv 本地翻译服务，返回译文。"""
    pass  # Task 5 实现


def extract_paragraphs(md_text: str) -> list[tuple[str, str]]:
    """
    从带标题的 MD 文本提取段落，返回 [(para_id, text), ...]。
    段落 ID 格式：{section_num}-p{index}，如 3.1.1-p0
    """
    pass  # Task 5 实现


def generate_translation_sidecar(
    main_md: str, out_path: Path, checkpoint_every: int = 5
) -> None:
    """生成翻译 sidecar JSON 文件，增量保存。"""
    pass  # Task 5 实现


# ---------------------------------------------------------------------------
# 主流程
# ---------------------------------------------------------------------------

def run(pdf_path: Path, model: str, do_translate: bool, translate_only: bool) -> None:
    pass  # Task 6 实现


def main() -> None:
    from dotenv import load_dotenv
    load_dotenv()

    parser = argparse.ArgumentParser(description="PDF → 结构化 Markdown")
    parser.add_argument("pdf", type=Path, help="输入 PDF 文件")
    parser.add_argument("--translate", action="store_true", help="生成翻译 sidecar")
    parser.add_argument("--translate-only", action="store_true",
                        help="只翻译（已有 main.md 时跳过转换）")
    parser.add_argument("--model", default="claude-haiku-4-5",
                        help="LLM 模型（默认 claude-haiku-4-5）")
    args = parser.parse_args()

    if not args.pdf.exists():
        print(f"错误：文件不存在: {args.pdf}", file=sys.stderr)
        sys.exit(1)

    run(args.pdf, args.model, args.translate, args.translate_only)


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 创建 `tools/pdf2md/README.md`**

```markdown
# pdf2md

学术 PDF → 结构化 Markdown 工具。

## 安装

```bash
pip install -r requirements.txt
```

## 配置

在工程根目录的 `.env` 中配置（仅翻译功能需要 LLM）：

```bash
ANTHROPIC_BASE_URL=...
ANTHROPIC_AUTH_TOKEN=...
```

## 用法

```bash
# 基本转换
python pdf2md.py paper.pdf

# 带翻译（需要 mdv 翻译服务在线：mdv server start）
python pdf2md.py paper.pdf --translate

# 只翻译（已有 main.md 时）
python pdf2md.py paper.pdf --translate-only

# 指定 LLM 模型
python pdf2md.py paper.pdf --model claude-opus-4-7
```

## 输出

```
paper/
  ├── manifest.json      # 元信息（版本、生成时间）
  ├── main.md            # 主文件（摘要 + 正文 + 参考文献）
  ├── main.translation.json  # 翻译 sidecar（--translate 时）
  ├── meta.json          # 论文元信息（标题、作者等）
  ├── appendix.md        # 附录
  ├── toc.json           # PDF 目录
  ├── raw.md             # 原始输出（debug）
  └── audit.jsonl        # LLM 调用记录
```
```

- [ ] **Step 4: 确认 pytest 可运行**

```bash
cd tools/pdf2md
pip install -r requirements.txt
python -m pytest tests/ -v 2>&1 | head -5
# 预期: "no tests ran" 或空目录提示，无报错
```

- [ ] **Step 5: Commit**

```bash
git add tools/pdf2md/
git commit -m "feat(pdf2md): scaffold project structure"
```

---

## Task 2: 论文拆分 (`split_paper` + `parse_meta`)

**Files:**
- Modify: `tools/pdf2md/pdf2md.py`（实现 `split_paper`、`parse_meta`）
- Create: `tools/pdf2md/tests/test_split.py`

这是核心逻辑，用真实的 Llama3 raw.md 片段作为测试数据。

- [ ] **Step 1: 写测试**

创建 `tools/pdf2md/tests/test_split.py`：

```python
import pytest
from pdf2md import split_paper, parse_meta

# 模拟 arxiv 论文的 markitdown 输出结构
SAMPLE_RAW = """\
4
2
0
2

v
o
N
2
3

The Llama 3 Herd of Models
Llama Team, AI @ Meta

Date: July 23, 2024
Website: https://llama.meta.com/

arXiv:2407.21783

Abstract

Modern artificial intelligence systems are powered by foundation models.
This paper presents a new set of foundation models, called Llama 3.

1 Introduction

Foundation models are general models of language, vision, speech.

The development of modern foundation models consists of two main stages.

2 General Overview

This section provides an overview of the Llama 3 development.

References

Amro Abbas et al. Semdedup. arXiv:2303.09540, 2023.

Contributors

Alice, Bob, Charlie contributed to this work.

Acknowledgements

We thank the team for their support.
"""


def test_split_body_contains_introduction():
    result = split_paper(SAMPLE_RAW)
    assert "Foundation models are general models" in result["body"]


def test_split_body_excludes_references():
    result = split_paper(SAMPLE_RAW)
    assert "Amro Abbas" not in result["body"]


def test_split_references_present():
    result = split_paper(SAMPLE_RAW)
    assert "Amro Abbas" in result["references"]


def test_split_abstract_present():
    result = split_paper(SAMPLE_RAW)
    assert "Modern artificial intelligence" in result["abstract"]


def test_split_abstract_excludes_body():
    result = split_paper(SAMPLE_RAW)
    assert "Foundation models are general models" not in result["abstract"]


def test_split_appendix_contains_contributors():
    result = split_paper(SAMPLE_RAW)
    assert "Alice, Bob, Charlie" in result["appendix"]


def test_split_appendix_contains_acknowledgements():
    result = split_paper(SAMPLE_RAW)
    assert "We thank the team" in result["appendix"]


def test_split_meta_lines_before_abstract():
    result = split_paper(SAMPLE_RAW)
    meta_text = "\n".join(result["meta_lines"])
    assert "The Llama 3 Herd of Models" in meta_text
    assert "Modern artificial intelligence" not in meta_text


def test_parse_meta_title():
    result = split_paper(SAMPLE_RAW)
    meta = parse_meta(result["meta_lines"])
    assert meta["title"] == "The Llama 3 Herd of Models"


def test_parse_meta_date():
    result = split_paper(SAMPLE_RAW)
    meta = parse_meta(result["meta_lines"])
    assert meta["date"] == "2024-07-23"


def test_parse_meta_arxiv_id():
    result = split_paper(SAMPLE_RAW)
    meta = parse_meta(result["meta_lines"])
    assert meta["arxiv_id"] == "2407.21783"


def test_parse_meta_url():
    result = split_paper(SAMPLE_RAW)
    meta = parse_meta(result["meta_lines"])
    assert meta["url"] == "https://arxiv.org/abs/2407.21783"


def test_split_no_abstract():
    """没有 Abstract 时，整个文本作为 body"""
    raw = "1 Introduction\n\nSome text here.\n\n2 Methods\n\nMore text.\n"
    result = split_paper(raw)
    assert result["abstract"] == ""
    assert "Some text here." in result["body"]


def test_split_no_references():
    """没有 References 时，references 为空"""
    raw = "Abstract\n\nSome abstract.\n\n1 Introduction\n\nSome text.\n"
    result = split_paper(raw)
    assert result["references"] == ""


def test_split_toc_region_excluded_from_abstract():
    """目录区域（Abstract 后的密集编号行）应被丢弃"""
    raw = """\
Abstract

Some abstract text here.

1 Introduction
2 Methods
3 Results
4 Conclusion

1 Introduction

Actual introduction text here.

References

Some ref.
"""
    result = split_paper(raw)
    # 目录区域的密集编号行不应出现在 abstract 中
    assert result["abstract"].strip() == "Some abstract text here."
    assert "Actual introduction text here." in result["body"]
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd tools/pdf2md
python -m pytest tests/test_split.py -v 2>&1 | head -20
# 预期: FAIL — split_paper returns None (pass 未实现)
```

- [ ] **Step 3: 实现 `split_paper` 和 `parse_meta`**

在 `tools/pdf2md/pdf2md.py` 中，替换 `split_paper` 和 `parse_meta` 的 `pass`：

```python
def split_paper(raw_text: str) -> dict:
    lines = raw_text.splitlines()
    n = len(lines)

    # --- 定位各边界行号 ---
    abstract_line = next(
        (i for i, l in enumerate(lines) if re.match(r'^Abstract\s*$', l.strip())), None
    )
    references_line = next(
        (i for i, l in enumerate(lines) if re.match(r'^References\s*$', l.strip())), None
    )
    appendix_line = next(
        (i for i, l in enumerate(lines)
         if re.match(r'^(Contributors|Acknowledgements|Appendix)\b', l.strip())), None
    )

    # --- 元信息：Abstract 之前 ---
    meta_end = abstract_line if abstract_line is not None else 0
    meta_lines = lines[:meta_end]

    # --- 正文开始：跳过目录区域 ---
    body_start = _find_body_start(lines, after=abstract_line)

    # --- 各部分边界 ---
    body_end = references_line if references_line is not None else (
        appendix_line if appendix_line is not None else n
    )
    refs_end = appendix_line if appendix_line is not None else n

    # --- abstract：Abstract 行之后到 body_start 之前，去掉目录区域 ---
    if abstract_line is not None and body_start is not None:
        abstract_lines = [
            l for l in lines[abstract_line + 1:body_start]
            if not re.match(r'^\d+\.?\s+\S', l)  # 去掉目录编号行
        ]
        abstract = "\n".join(abstract_lines).strip()
    else:
        abstract = ""

    # --- body ---
    if body_start is not None:
        body = "\n".join(lines[body_start:body_end]).strip()
    else:
        body = raw_text.strip()

    # --- references ---
    if references_line is not None:
        references = "\n".join(lines[references_line + 1:refs_end]).strip()
    else:
        references = ""

    # --- appendix ---
    if appendix_line is not None:
        appendix = "\n".join(lines[appendix_line:n]).strip()
    else:
        appendix = ""

    return {
        "meta_lines": meta_lines,
        "abstract": abstract,
        "body": body,
        "references": references,
        "appendix": appendix,
    }


def _find_body_start(lines: list[str], after: int | None) -> int | None:
    """
    在 after 行之后找到正文第一章的行号。
    正文第一章特征：`^\d+\.?\s+\S`，如 "1 Introduction" 或 "1. Overview"
    目录区域特征：连续出现多个这样的行（密集编号行）→ 找到密集区域结束后的第一个编号行
    """
    start = (after + 1) if after is not None else 0

    # 收集 after 之后所有编号行的位置
    numbered = [
        i for i in range(start, len(lines))
        if re.match(r'^\d+\.?\s+\S', lines[i].strip())
    ]
    if not numbered:
        return None

    # 检测目录区域：前 N 个编号行如果彼此间距很小（≤3行），认为是目录
    # 找到间距突然变大的位置，之后的第一个编号行是真正的正文开始
    if len(numbered) <= 1:
        return numbered[0]

    toc_end_idx = 0  # 默认第一个编号行就是正文
    for i in range(1, len(numbered)):
        gap = numbered[i] - numbered[i - 1]
        if gap > 5:  # 间距 > 5 行，认为目录区域结束
            toc_end_idx = i
            break

    return numbered[toc_end_idx]


def parse_meta(meta_lines: list[str]) -> dict:
    text = "\n".join(meta_lines)

    # 标题：找第一个长度 > 10 且不是纯数字/字母的非空行（跳过 arxiv 版本号噪音）
    title = None
    for line in meta_lines:
        line = line.strip()
        if len(line) > 10 and not re.match(r'^[\d\s\w]{1,5}$', line):
            title = line
            break

    # 日期：匹配 "Date: Month DD, YYYY"
    date = None
    date_match = re.search(r'Date:\s+(\w+)\s+(\d+),\s+(\d{4})', text)
    if date_match:
        from datetime import datetime
        try:
            dt = datetime.strptime(f"{date_match.group(1)} {date_match.group(2)} {date_match.group(3)}", "%B %d %Y")
            date = dt.strftime("%Y-%m-%d")
        except ValueError:
            pass

    # arXiv ID：匹配 "arXiv:NNNN.NNNNN" 或独立出现的 NNNN.NNNNN
    arxiv_id = None
    arxiv_match = re.search(r'(?:arXiv:|arxiv:)?(\d{4}\.\d{4,5})', text)
    if arxiv_match:
        arxiv_id = arxiv_match.group(1)

    return {
        "title": title,
        "authors": [],  # 作者格式多变，暂不解析
        "date": date,
        "arxiv_id": arxiv_id,
        "url": f"https://arxiv.org/abs/{arxiv_id}" if arxiv_id else None,
    }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd tools/pdf2md
python -m pytest tests/test_split.py -v
# 预期: 全部 PASS（14 个测试）
```

如果有失败，检查边界逻辑，调整 `_find_body_start` 的间距阈值（当前为 5）。

- [ ] **Step 5: Commit**

```bash
git add tools/pdf2md/pdf2md.py tools/pdf2md/tests/test_split.py
git commit -m "feat(pdf2md): implement paper splitting and meta parsing"
```

---

## Task 3: TOC 提取 (`extract_toc`)

**Files:**
- Modify: `tools/pdf2md/pdf2md.py`（实现 `extract_toc`）
- Create: `tools/pdf2md/tests/test_toc.py`

- [ ] **Step 1: 写测试**

创建 `tools/pdf2md/tests/test_toc.py`：

```python
import pytest
from unittest.mock import MagicMock, patch
from pathlib import Path
from pdf2md import extract_toc


def _make_outline_item(title: str, page: int, children=None):
    """构造 pypdf outline item mock"""
    item = MagicMock()
    item.title = title
    item.__class__.__name__ = "Destination"
    # pypdf 的 page 是一个引用对象，page_number 是实际页码
    page_obj = MagicMock()
    page_obj.page_number = page - 1  # pypdf 从 0 开始
    item.page = page_obj
    return item


def test_extract_toc_flat():
    """扁平 outline（无嵌套）"""
    outline = [
        _make_outline_item("Introduction", 1),
        _make_outline_item("Methods", 5),
        _make_outline_item("Results", 10),
    ]
    with patch("pdf2md.PdfReader") as mock_reader:
        mock_reader.return_value.outline = outline
        result = extract_toc(Path("fake.pdf"))

    assert len(result) == 3
    assert result[0] == {"num": "1", "title": "Introduction", "level": 1, "pageNum": 1}
    assert result[1] == {"num": "2", "title": "Methods", "level": 1, "pageNum": 5}
    assert result[2] == {"num": "3", "title": "Results", "level": 1, "pageNum": 10}


def test_extract_toc_nested():
    """嵌套 outline（二级标题）"""
    outline = [
        _make_outline_item("Introduction", 1),
        [
            _make_outline_item("Background", 2),
            _make_outline_item("Related Work", 3),
        ],
        _make_outline_item("Methods", 5),
    ]
    with patch("pdf2md.PdfReader") as mock_reader:
        mock_reader.return_value.outline = outline
        result = extract_toc(Path("fake.pdf"))

    nums = [e["num"] for e in result]
    assert "1" in nums
    assert "1.1" in nums
    assert "1.2" in nums
    assert "2" in nums


def test_extract_toc_empty_outline():
    """PDF 没有 outline 时返回空列表"""
    with patch("pdf2md.PdfReader") as mock_reader:
        mock_reader.return_value.outline = []
        result = extract_toc(Path("fake.pdf"))
    assert result == []


def test_extract_toc_levels():
    """level 字段正确"""
    outline = [
        _make_outline_item("Chapter", 1),
        [
            _make_outline_item("Section", 2),
            [
                _make_outline_item("Subsection", 3),
            ],
        ],
    ]
    with patch("pdf2md.PdfReader") as mock_reader:
        mock_reader.return_value.outline = outline
        result = extract_toc(Path("fake.pdf"))

    levels = {e["title"]: e["level"] for e in result}
    assert levels["Chapter"] == 1
    assert levels["Section"] == 2
    assert levels["Subsection"] == 3
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd tools/pdf2md
python -m pytest tests/test_toc.py -v 2>&1 | head -15
# 预期: FAIL — extract_toc returns None
```

- [ ] **Step 3: 实现 `extract_toc`**

在 `tools/pdf2md/pdf2md.py` 顶部 import 区域添加：
```python
from pypdf import PdfReader
```

替换 `extract_toc` 的 `pass`：

```python
def extract_toc(pdf_path: Path) -> list[dict]:
    reader = PdfReader(str(pdf_path))
    outline = reader.outline
    if not outline:
        return []

    result: list[dict] = []
    counters: list[int] = []  # 每层的当前计数

    def _walk(items: list, depth: int) -> None:
        # 确保 counters 有足够深度
        while len(counters) <= depth:
            counters.append(0)
        # 重置更深层的计数器
        while len(counters) > depth + 1:
            counters.pop()

        for item in items:
            if isinstance(item, list):
                # 嵌套列表：递归处理子节点（上一个 item 是父节点）
                _walk(item, depth + 1)
            else:
                counters[depth] += 1
                # 重置更深层（如果有）
                while len(counters) > depth + 1:
                    counters.pop()

                num = ".".join(str(c) for c in counters[:depth + 1])
                try:
                    page_num = item.page.page_number + 1  # pypdf 从 0 开始
                except Exception:
                    page_num = 0

                result.append({
                    "num": num,
                    "title": item.title,
                    "level": depth + 1,
                    "pageNum": page_num,
                })

    _walk(outline, 0)
    return result
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd tools/pdf2md
python -m pytest tests/test_toc.py -v
# 预期: 全部 PASS（4 个测试）
```

- [ ] **Step 5: Commit**

```bash
git add tools/pdf2md/pdf2md.py tools/pdf2md/tests/test_toc.py
git commit -m "feat(pdf2md): implement TOC extraction from PDF outline"
```

---

## Task 4: 标题插入 (`insert_headings`)

**Files:**
- Modify: `tools/pdf2md/pdf2md.py`（实现 `insert_headings`）
- Create: `tools/pdf2md/tests/test_structure.py`

- [ ] **Step 1: 写测试**

创建 `tools/pdf2md/tests/test_structure.py`：

```python
import pytest
from pathlib import Path
import tempfile
from pdf2md import insert_headings

BODY = """\
Foundation models are general models of language.

The development of modern foundation models consists of two main stages.

Pre-Training Data

We curate data from the web using a multi-stage pipeline.

Web Data Curation

We process web data using the following steps.

Determining the Data Mix

We use a data mix that balances quality and diversity.
"""

TOC = [
    {"num": "1", "title": "Introduction", "level": 1, "pageNum": 1},
    {"num": "3", "title": "Pre-Training", "level": 1, "pageNum": 4},
    {"num": "3.1", "title": "Pre-Training Data", "level": 2, "pageNum": 4},
    {"num": "3.1.1", "title": "Web Data Curation", "level": 3, "pageNum": 4},
    {"num": "3.1.2", "title": "Determining the Data Mix", "level": 3, "pageNum": 6},
]


def test_insert_exact_match():
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        audit_path = Path(f.name)
    result = insert_headings(BODY, TOC, audit_path)
    assert "## 3.1. Pre-Training Data" in result
    assert "### 3.1.1. Web Data Curation" in result
    assert "### 3.1.2. Determining the Data Mix" in result


def test_insert_heading_before_text():
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        audit_path = Path(f.name)
    result = insert_headings(BODY, TOC, audit_path)
    lines = result.splitlines()
    # 标题行应在对应文字行之前
    heading_idx = next(i for i, l in enumerate(lines) if "## 3.1. Pre-Training Data" in l)
    text_idx = next(i for i, l in enumerate(lines) if "We curate data from the web" in l)
    assert heading_idx < text_idx


def test_insert_not_found_recorded_in_audit():
    """找不到的标题记录在 audit.jsonl 中"""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        audit_path = Path(f.name)
    toc_with_missing = TOC + [
        {"num": "99", "title": "Nonexistent Section XYZ", "level": 1, "pageNum": 99}
    ]
    insert_headings(BODY, toc_with_missing, audit_path)
    import json
    entries = [json.loads(l) for l in audit_path.read_text().splitlines() if l.strip()]
    not_found = [e for e in entries if e.get("strategy") == "not_found"]
    assert any("Nonexistent Section XYZ" in e.get("section", "") for e in not_found)


def test_insert_fuzzy_match():
    """标题文字有标点差异时模糊匹配"""
    body = "Pre-Training: Data Preparation\n\nSome content here.\n"
    toc = [{"num": "3.1", "title": "Pre-Training Data Preparation", "level": 2, "pageNum": 4}]
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        audit_path = Path(f.name)
    result = insert_headings(body, toc, audit_path)
    assert "## 3.1. Pre-Training Data Preparation" in result


def test_insert_preserves_body_content():
    """插入标题后原文内容完整保留"""
    with tempfile.NamedTemporaryFile(suffix=".jsonl", delete=False) as f:
        audit_path = Path(f.name)
    result = insert_headings(BODY, TOC, audit_path)
    assert "Foundation models are general models" in result
    assert "We process web data using the following steps." in result
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd tools/pdf2md
python -m pytest tests/test_structure.py -v 2>&1 | head -15
# 预期: FAIL — insert_headings returns None
```

- [ ] **Step 3: 实现 `insert_headings`**

替换 `insert_headings` 的 `pass`：

```python
def insert_headings(body: str, toc: list[dict], audit_path: Path) -> str:
    lines = body.splitlines()
    # 记录每个 TOC 节点的插入位置：{toc_index: line_index}
    insertions: dict[int, int] = {}
    last_found_line = 0

    for toc_idx, entry in enumerate(toc):
        title = entry["title"]
        found_line = _find_title_line(lines, title, after=last_found_line)

        if found_line is not None:
            insertions[toc_idx] = found_line
            last_found_line = found_line
        else:
            # 记录 not_found 到 audit
            _append_audit(audit_path, {
                "ts": datetime.now(timezone.utc).isoformat(),
                "step": "structure",
                "section": f"{entry['num']}. {title}",
                "strategy": "not_found",
            })

    # 按行号倒序插入（避免行号偏移）
    result_lines = lines[:]
    for toc_idx in sorted(insertions.keys(), key=lambda i: insertions[i], reverse=True):
        entry = toc[toc_idx]
        level = entry["level"]
        prefix = "#" * level
        heading_line = f"{prefix} {entry['num']}. {entry['title']}"
        insert_at = insertions[toc_idx]
        result_lines.insert(insert_at, heading_line)

    return "\n".join(result_lines)


def _find_title_line(lines: list[str], title: str, after: int) -> int | None:
    """策略 A: 精确匹配；策略 B: 模糊匹配（去标点 + 前4词）"""
    # 策略 A: 精确匹配
    for i in range(after, len(lines)):
        if title in lines[i]:
            return i

    # 策略 B: 去标点后匹配
    title_clean = re.sub(r'[^\w\s]', ' ', title).lower()
    for i in range(after, len(lines)):
        line_clean = re.sub(r'[^\w\s]', ' ', lines[i]).lower()
        if title_clean in line_clean:
            return i

    # 策略 B2: 只匹配前 4 个词
    words = title.split()[:4]
    if len(words) >= 2:
        prefix_pattern = r'\s+'.join(re.escape(w) for w in words)
        for i in range(after, len(lines)):
            if re.search(prefix_pattern, lines[i], re.IGNORECASE):
                return i

    return None


def _append_audit(audit_path: Path, entry: dict) -> None:
    with audit_path.open("a", encoding="utf-8") as f:
        f.write(json.dumps(entry, ensure_ascii=False) + "\n")
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd tools/pdf2md
python -m pytest tests/test_structure.py -v
# 预期: 全部 PASS（5 个测试）
```

- [ ] **Step 5: Commit**

```bash
git add tools/pdf2md/pdf2md.py tools/pdf2md/tests/test_structure.py
git commit -m "feat(pdf2md): implement heading insertion with exact/fuzzy matching"
```

---

## Task 5: 翻译 sidecar (`translate_text` + `extract_paragraphs` + `generate_translation_sidecar`)

**Files:**
- Modify: `tools/pdf2md/pdf2md.py`（实现三个翻译相关函数）

翻译逻辑直接移植自 `translate_sidecar.py`，段落提取逻辑与 `paragraph-mapper.ts` 保持一致（基于标题行 `^#{1,3}\s+\d` 分节）。

- [ ] **Step 1: 实现 `translate_text`**

替换 `translate_text` 的 `pass`：

```python
def translate_text(text: str) -> str:
    payload = json.dumps({"text": text}).encode()
    req = urllib.request.Request(
        TRANSLATE_API,
        data=payload,
        headers={"Content-Type": "application/json"},
        method="POST",
    )
    with urllib.request.urlopen(req, timeout=30) as resp:
        data = json.loads(resp.read())
        return data["translatedText"]
```

- [ ] **Step 2: 实现 `extract_paragraphs`**

替换 `extract_paragraphs` 的 `pass`：

```python
def extract_paragraphs(md_text: str) -> list[tuple[str, str]]:
    lines = md_text.splitlines()
    section_stack: list[tuple[int, str]] = []  # [(level, num_str)]
    para_counts: dict[str, int] = {}
    paragraphs: list[tuple[str, str]] = []
    current_para: list[str] = []

    def flush():
        if not current_para:
            return
        text = "\n".join(current_para).strip()
        current_para.clear()
        if not text or len(text) < 20:
            return
        if text.startswith("> **[Figure") or text.startswith("> **[Table"):
            return
        if text.startswith("$$"):
            return
        path = ".".join(n for _, n in section_stack) if section_stack else "0"
        idx = para_counts.get(path, 0)
        para_counts[path] = idx + 1
        paragraphs.append((f"{path}-p{idx}", text))

    for line in lines:
        m = re.match(r'^(#{1,3})\s+(\d+(?:\.\d+)*)\.?\s+', line)
        if m:
            flush()
            level = len(m.group(1))
            num = m.group(2)
            while section_stack and section_stack[-1][0] >= level:
                section_stack.pop()
            section_stack.append((level, num))
            continue
        if not line.strip():
            flush()
            continue
        current_para.append(line)

    flush()
    return paragraphs
```

- [ ] **Step 3: 实现 `generate_translation_sidecar`**

替换 `generate_translation_sidecar` 的 `pass`：

```python
def generate_translation_sidecar(
    main_md: str, out_path: Path, checkpoint_every: int = 5
) -> None:
    paragraphs = extract_paragraphs(main_md)
    print(f"  共提取 {len(paragraphs)} 个段落")

    existing: dict[str, str] = {}
    if out_path.exists():
        existing = json.loads(out_path.read_text(encoding="utf-8"))
        print(f"  已有翻译 {len(existing)} 条，跳过已翻译段落")

    new_count = 0
    for i, (para_id, text) in enumerate(paragraphs):
        if para_id in existing:
            continue
        text_to_translate = text[:500] + "..." if len(text) > 500 else text
        try:
            result = translate_text(text_to_translate)
            existing[para_id] = result
            new_count += 1
            print(f"  [{i+1}/{len(paragraphs)}] {para_id}: {text[:40]!r} → {result[:30]!r}")
            if new_count % checkpoint_every == 0:
                out_path.write_text(
                    json.dumps(existing, ensure_ascii=False, indent=2), encoding="utf-8"
                )
        except Exception as e:
            print(f"  [{i+1}/{len(paragraphs)}] {para_id} 翻译失败: {e}")
        time.sleep(0.05)

    out_path.write_text(
        json.dumps(existing, ensure_ascii=False, indent=2), encoding="utf-8"
    )
    print(f"  完成！新增 {new_count} 条，总计 {len(existing)} 条")
```

- [ ] **Step 4: 手动验证翻译函数可调用（不需要服务在线）**

```bash
cd tools/pdf2md
python -c "
from pdf2md import extract_paragraphs
text = '''# 1. Introduction

Foundation models are general models of language.

The development of modern foundation models consists of two main stages.

## 3.1. Pre-Training Data

We curate data from the web.
'''
paras = extract_paragraphs(text)
for pid, t in paras:
    print(pid, repr(t[:50]))
"
# 预期输出类似:
# 1-p0 'Foundation models are general models of language.'
# 1-p1 'The development of modern foundation models consi'
# 3.1-p0 'We curate data from the web.'
```

- [ ] **Step 5: Commit**

```bash
git add tools/pdf2md/pdf2md.py
git commit -m "feat(pdf2md): implement translation sidecar generation"
```

---

## Task 6: 主入口 `run()` + 端到端验证

**Files:**
- Modify: `tools/pdf2md/pdf2md.py`（实现 `run()`）

- [ ] **Step 1: 实现 `run()`**

替换 `run` 的 `pass`：

```python
def run(pdf_path: Path, model: str, do_translate: bool, translate_only: bool) -> None:
    out_dir = pdf_path.parent / pdf_path.stem
    out_dir.mkdir(exist_ok=True)
    audit_path = out_dir / "audit.jsonl"

    if not translate_only:
        # Step 1: markitdown → raw.md
        print(f"[1/5] markitdown 转换: {pdf_path.name}")
        from markitdown import MarkItDown
        raw_text = MarkItDown().convert(str(pdf_path)).text_content
        (out_dir / "raw.md").write_text(raw_text, encoding="utf-8")
        print(f"      raw.md: {len(raw_text.splitlines())} 行")

        # Step 2: 拆分论文
        print("[2/5] 拆分论文各部分")
        parts = split_paper(raw_text)
        meta = parse_meta(parts["meta_lines"])
        (out_dir / "meta.json").write_text(
            json.dumps(meta, ensure_ascii=False, indent=2), encoding="utf-8"
        )
        if parts["appendix"]:
            (out_dir / "appendix.md").write_text(parts["appendix"], encoding="utf-8")
        print(f"      abstract: {len(parts['abstract'])} chars, "
              f"body: {len(parts['body'].splitlines())} 行, "
              f"references: {len(parts['references'])} chars")

        # Step 3: 提取 TOC
        print("[3/5] 提取 TOC")
        toc = extract_toc(pdf_path)
        (out_dir / "toc.json").write_text(
            json.dumps(toc, ensure_ascii=False, indent=2), encoding="utf-8"
        )
        print(f"      {len(toc)} 个章节")

        # Step 4: 插入标题
        print("[4/5] 插入章节标题")
        body_with_headings = insert_headings(parts["body"], toc, audit_path)

        # 组合 main.md
        sections = []
        if parts["abstract"]:
            sections.append(f"## Abstract\n\n{parts['abstract']}")
        sections.append(body_with_headings)
        if parts["references"]:
            sections.append(f"## References\n\n{parts['references']}")
        main_md = "\n\n---\n\n".join(sections)
        (out_dir / "main.md").write_text(main_md, encoding="utf-8")
        print(f"      main.md: {len(main_md.splitlines())} 行")

        # manifest
        (out_dir / "manifest.json").write_text(
            json.dumps({
                "version": 1,
                "pdf": pdf_path.name,
                "created_at": datetime.now(timezone.utc).isoformat(),
            }, indent=2),
            encoding="utf-8",
        )

    # Step 5: 翻译 sidecar
    if do_translate or translate_only:
        main_md_path = out_dir / "main.md"
        if not main_md_path.exists():
            print("错误：main.md 不存在，请先运行转换（去掉 --translate-only）", file=sys.stderr)
            sys.exit(1)
        print("[5/5] 生成翻译 sidecar")
        try:
            generate_translation_sidecar(
                main_md_path.read_text(encoding="utf-8"),
                out_dir / "main.translation.json",
            )
        except urllib.error.URLError:
            print("  警告：mdv 翻译服务不在线，跳过翻译（请先运行 mdv server start）")

    print(f"\n完成！输出目录: {out_dir}/")
```

- [ ] **Step 2: 用真实 PDF 端到端测试**

```bash
cd tools/pdf2md
python pdf2md.py /Users/huanghao/workspace/walle/bots-tmp/0408-llm-wiki/raw/inbox/llama-3-herd-2407.21783.pdf
```

验证：
- `llama-3-herd-2407.21783/raw.md` 存在且有内容
- `llama-3-herd-2407.21783/main.md` 存在，包含章节标题（`# 1.`、`## 3.1.` 等）
- `llama-3-herd-2407.21783/meta.json` 包含 `arxiv_id: "2407.21783"`
- `llama-3-herd-2407.21783/toc.json` 包含多个章节
- `llama-3-herd-2407.21783/manifest.json` 存在

```bash
# 快速检查
ls llama-3-herd-2407.21783/
grep "arxiv_id" llama-3-herd-2407.21783/meta.json
grep "^# " llama-3-herd-2407.21783/main.md | head -5
```

- [ ] **Step 3: 运行全部测试**

```bash
cd tools/pdf2md
python -m pytest tests/ -v
# 预期: 全部 PASS
```

- [ ] **Step 4: Commit**

```bash
git add tools/pdf2md/pdf2md.py
git commit -m "feat(pdf2md): implement main run() pipeline and end-to-end flow"
```

---

## 自检

**Spec coverage:**
- ✅ markitdown → raw.md（Task 6 Step 1）
- ✅ split_paper: abstract/body/references/appendix/meta（Task 2）
- ✅ meta.json 解析（Task 2）
- ✅ toc.json 提取（Task 3）
- ✅ 标题插入 策略A/B/C（Task 4）
- ✅ audit.jsonl not_found 记录（Task 4）
- ✅ 翻译 sidecar 生成（Task 5）
- ✅ manifest.json（Task 6）
- ✅ appendix.md（Task 6）
- ✅ --translate / --translate-only / --model CLI 参数（Task 1 骨架 + Task 6）
- ✅ 环境变量通过 dotenv 加载（Task 1 骨架 main()）
- ✅ mdv 服务不在线时跳过翻译（Task 6）
- ✅ requirements.txt + .python-version（Task 1）
- ✅ README.md（Task 1）

**Placeholder scan:** 无 TBD/TODO/placeholder。

**Type consistency:** `split_paper` 返回 `dict`，Task 2/6 均一致；`extract_toc` 返回 `list[dict]`，Task 3/4/6 一致；`insert_headings(body, toc, audit_path)` 签名 Task 4/6 一致。
