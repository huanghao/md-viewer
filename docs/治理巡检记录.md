# 治理巡检记录

## 2026-03-02 首次治理

范围：
- `README.md`
- `AGENTS.md`
- `docs/`
- `docs/design/`

本次完成：
1. 新增文档治理基线：
   - `docs/README.md`
   - `docs/文档治理规范.md`
   - `docs/文档模板.md`
2. 修复巡检发现的文档链接问题：
   - `docs/design/config-strategy.md` 中 `man` 相对路径错误（`../man` -> `../../man`）
   - `docs/design/html-support.md` 中原型链接从绝对路径改为仓库内相对路径
   - `docs/design/sync-unified-state.md` 中原型链接从绝对路径改为仓库内相对路径
   - `docs/design/sync-unified-state.md` 中示例文案使用代码样式，避免被误识别为实际链接

巡检结论：
- 当前扫描范围内未发现新的相对链接断链。
- 文档分层已建立，但历史文档仍主要位于 `docs/` 根目录，后续可按主题继续收敛。

后续待办（治理）：
1. 建立 `docs/archive/YYYY-MM/` 并按“冻结/进行中”原则迁移阶段性文档。
2. 为 `docs/design/` 增加轻量索引页，降低查找成本。
3. 在 CI 中加入文档相对链接检查，避免回归。
