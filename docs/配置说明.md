# 配置说明

本项目配置的权威说明以 man page 为准：

- `man/md-viewer.config.5`

补充设计文档：

- `docs/design/config-strategy.md`

## 配置变更时的最低要求

1. 更新 `man/md-viewer.config.5`（默认值、范围、兼容性说明）
2. 同步更新 `docs/design/config-strategy.md`（如有设计变化）
3. 更新配置加载代码
4. 如涉及废弃项，保留向后兼容并在文档标注
