# 待完成任务

## 搜索路径现在很慢是为什么


## PDF 选中功能交互式验证实验
写一个小的测试页面，打开某个 PDF，设计若干点击实验（正文词、标题、公式字符、行顶部/底部边缘等），用户点击后记录 log（命中的 item 索引、str、fontH 来源、包围盒坐标），用户提供反馈（选中是否符合预期）。通过这些真实数据验证并改进选中逻辑。建议在 pdf-item-analysis 脚本完成后进行。

## PDF TextItem 粒度数据分析
对多份真实 PDF 文件（学术论文、技术报告等）做定量分析，统计各类内容（正文、标题、页眉页脚、表格、公式）的 TextItem 粒度分布，补充到 docs/pdf-viewer-internals.md 的 4.4 节。

## PDF 懒渲染性能基准测试
建立基准测试，记录以下指标在不同 PDF 大小/页数下的表现：白屏率、首页可见时间、内存峰值（通过 getRenderedCount() 监控）、渲染抖动。可顺带考虑在 debug 模式下直接在页面上可视化 TextLayer overlay。

## PDF 阅读体验改进
1）PDF 页面滚动时没有显式滚动条
2）PDF 页面内没有当前页数/总页数提示，也没有跳转到指定页的快捷方式


## 刷新后的高亮
1）高亮具体内容，而不是整个屏幕亮一下然后消失。做一下设计跟我对齐
2）有好几个地方都存在这种模式，请总结一下。当前状态 vs 刷新后状态变更 vs 实际状态和mdv维护的当前状态。你帮我深入分析一下本质问题是什么吗？和docs里的`docs/research/20260302-state-sync-risk-analysis.md`内容进行整合。


- 翻译服务重启，后台服务重启
- 页面zoom in/out
- 分析代码里的magic number，看看是不是要做一个完整的配置页面
- 焦点视图里加一个文本类型筛选，默认不要json（大量agent自动生成的文档），需要原型设计
* diff模式改一下，再原文档里给一些淡淡的背景区别提示即可，或者再段落的左边给一些状态提示即可。但这意味着默认展示的不是老版本，而是新版本。但还需要diff信息。设计一下这个交互逻辑
* 评论是和当前文档关联的，但翻译没有
* 点开md的初始位置在哪里，好像不是开头，也不是上一次的查看位置。因为我发现新文件第一次点开就不是开头。先梳理一下现在的逻辑是什么。加测试
* 排序，文件夹放前面，然后再文件，字母顺序，忽略大小写。英文在前，中文在后。如果是数字开头的，需要用数字排序，而不是字符串
* 整理完文档规则
* 分析历史对话偏好，删除历史对话记录
* 学会用memory（还是说不用管）
* ❯ just build-client && just dev
bun run build.ts && bun run scripts/embed-client.ts
我需要一个一键watch的命令，现在just里的命令太多了，需要整理一下
- 给PDF生成目录
