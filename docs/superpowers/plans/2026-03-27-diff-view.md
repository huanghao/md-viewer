# Diff View Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 当文件有 M 标记时，在工具栏显示 `[± Diff]` 按钮，点击后展示当前展示内容 vs 磁盘最新内容的行级 diff 视图，用户可选择接受更新或关闭。

**Architecture:** 在 `FileInfo` 上新增 `pendingContent` 字段缓存磁盘最新内容；diff 算法（Myers line-level）放在独立的 `src/client/utils/diff.ts`；diff 视图渲染和状态管理在 `main.ts` 中；工具栏按钮在 `html.ts` 中添加，样式在 `css.ts` 中添加。

**Tech Stack:** TypeScript, 纯 JS Myers diff 算法（无外部依赖），现有 esbuild 构建流程。

---

### Task 1: 添加 `pendingContent` 字段到 `FileInfo` 类型

**Files:**
- Modify: `src/client/types.ts`

- [ ] **Step 1: 修改 `FileInfo` 接口，添加 `pendingContent` 字段**

在 `src/client/types.ts` 的 `FileInfo` 接口中添加：

```ts
export interface FileInfo {
  path: string;
  name: string;
  content: string;
  lastModified: number;
  displayedModified: number;
  isRemote: boolean;
  displayName?: string;
  isMissing?: boolean;
  pendingContent?: string;  // 磁盘最新内容（diff 视图使用，内存缓存）
}
```

- [ ] **Step 2: 确认构建无错误**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

Expected: `✅ Build completed!`

---

### Task 2: 实现 Myers line-level diff 算法

**Files:**
- Create: `src/client/utils/diff.ts`

- [ ] **Step 1: 创建 `src/client/utils/diff.ts`**

```ts
// Myers diff 算法 — line-level diff
// 返回 DiffLine 数组，type: 'equal' | 'insert' | 'delete'

export interface DiffLine {
  type: 'equal' | 'insert' | 'delete';
  content: string;
  oldLineNo?: number;  // 原文件行号（1-based，equal/delete 有值）
  newLineNo?: number;  // 新文件行号（1-based，equal/insert 有值）
}

export function diffLines(oldText: string, newText: string): DiffLine[] {
  const oldLines = oldText.split('\n');
  const newLines = newText.split('\n');
  const n = oldLines.length;
  const m = newLines.length;

  // Myers diff: 计算最短编辑路径
  const max = n + m;
  const v: number[] = new Array(2 * max + 1).fill(0);
  const trace: number[][] = [];

  outer: for (let d = 0; d <= max; d++) {
    trace.push([...v]);
    for (let k = -d; k <= d; k += 2) {
      const idx = k + max;
      let x: number;
      if (k === -d || (k !== d && v[idx - 1] < v[idx + 1])) {
        x = v[idx + 1];
      } else {
        x = v[idx - 1] + 1;
      }
      let y = x - k;
      while (x < n && y < m && oldLines[x] === newLines[y]) {
        x++;
        y++;
      }
      v[idx] = x;
      if (x >= n && y >= m) break outer;
    }
  }

  // 回溯路径
  const ops: Array<{ type: 'equal' | 'insert' | 'delete'; x: number; y: number }> = [];
  let x = n;
  let y = m;
  for (let d = trace.length - 1; d >= 0 && (x > 0 || y > 0); d--) {
    const vd = trace[d];
    const k = x - y;
    const idx = k + max;
    let prevK: number;
    if (k === -d || (k !== d && vd[idx - 1] < vd[idx + 1])) {
      prevK = k + 1;
    } else {
      prevK = k - 1;
    }
    const prevX = vd[prevK + max];
    const prevY = prevX - prevK;

    while (x > prevX + 1 && y > prevY + 1) {
      ops.unshift({ type: 'equal', x: x - 1, y: y - 1 });
      x--;
      y--;
    }
    if (d > 0) {
      if (x === prevX + 1 && y === prevY + 1 && prevX >= 0 && prevY >= 0 &&
          oldLines[prevX] === newLines[prevY]) {
        ops.unshift({ type: 'equal', x: prevX, y: prevY });
      } else if (x > prevX) {
        ops.unshift({ type: 'delete', x: prevX, y: -1 });
      } else {
        ops.unshift({ type: 'insert', x: -1, y: prevY });
      }
    }
    x = prevX;
    y = prevY;
  }

  // 转换为 DiffLine
  const result: DiffLine[] = [];
  let oldNo = 1;
  let newNo = 1;
  for (const op of ops) {
    if (op.type === 'equal') {
      result.push({ type: 'equal', content: oldLines[op.x], oldLineNo: oldNo++, newLineNo: newNo++ });
    } else if (op.type === 'delete') {
      result.push({ type: 'delete', content: oldLines[op.x], oldLineNo: oldNo++ });
    } else {
      result.push({ type: 'insert', content: newLines[op.y], newLineNo: newNo++ });
    }
  }
  return result;
}
```

- [ ] **Step 2: 确认构建无错误**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

Expected: `✅ Build completed!`

---

### Task 3: 在 `html.ts` 中添加 Diff 按钮

**Files:**
- Modify: `src/client/html.ts`

- [ ] **Step 1: 在 refreshButton 之前添加 diffButton**

在 `src/client/html.ts` 中，找到：
```html
        <button class="toolbar-text-button" id="refreshButton" onclick="handleRefreshButtonClick()" style="display: none;" title="文件已更新，点击刷新">
          <span id="refreshButtonText">[↻ 刷新]</span>
        </button>
```

改为：
```html
        <button class="toolbar-text-button" id="diffButton" onclick="handleDiffButtonClick()" style="display: none;" title="查看修改内容">
          <span id="diffButtonText">[± Diff]</span>
        </button>
        <button class="toolbar-text-button" id="refreshButton" onclick="handleRefreshButtonClick()" style="display: none;" title="文件已更新，点击刷新">
          <span id="refreshButtonText">[↻ 刷新]</span>
        </button>
```

- [ ] **Step 2: 确认构建无错误**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

---

### Task 4: 添加 diff 视图 CSS 样式

**Files:**
- Modify: `src/client/css.ts`

- [ ] **Step 1: 在 `/* 内容刷新闪烁动画 */` 注释之前添加 diff 视图样式**

找到 `/* 内容刷新闪烁动画 */`，在其前面插入：

```css
    /* Diff 视图 */
    .diff-view {
      font-family: 'SFMono-Regular', 'Menlo', 'Monaco', 'Consolas', monospace;
      font-size: 13px;
      line-height: 1.5;
      overflow-x: auto;
    }
    .diff-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 10px 16px;
      background: #f6f8fa;
      border-bottom: 1px solid #e1e4e8;
      font-size: 13px;
      color: #57606a;
      flex-shrink: 0;
    }
    .diff-header-titles {
      display: flex;
      gap: 0;
      flex: 1;
    }
    .diff-header-old,
    .diff-header-new {
      flex: 1;
      padding: 0 8px;
      font-size: 12px;
    }
    .diff-header-old { color: #b42318; }
    .diff-header-new { color: #1a7f37; }
    .diff-actions {
      display: flex;
      gap: 8px;
      flex-shrink: 0;
    }
    .diff-accept-btn {
      padding: 5px 12px;
      border-radius: 6px;
      border: 1px solid #2da44e;
      background: #2da44e;
      color: #fff;
      font-size: 12px;
      cursor: pointer;
      white-space: nowrap;
    }
    .diff-accept-btn:hover {
      background: #218a3e;
      border-color: #218a3e;
    }
    .diff-close-btn {
      padding: 5px 12px;
      border-radius: 6px;
      border: 1px solid #d0d7de;
      background: #fff;
      color: #24292f;
      font-size: 12px;
      cursor: pointer;
      white-space: nowrap;
    }
    .diff-close-btn:hover {
      background: #f6f8fa;
    }
    .diff-table {
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed;
    }
    .diff-table td {
      padding: 1px 8px;
      vertical-align: top;
      white-space: pre-wrap;
      word-break: break-all;
      width: 50%;
    }
    .diff-line-no {
      width: 40px !important;
      min-width: 40px;
      max-width: 40px;
      color: #8b949e;
      text-align: right;
      padding-right: 12px !important;
      user-select: none;
      font-size: 11px;
    }
    .diff-row-delete td {
      background: #ffebe9;
      color: #b42318;
    }
    .diff-row-delete .diff-line-no {
      background: #ffd6d1;
      color: #b42318;
    }
    .diff-row-insert td {
      background: #e6ffec;
      color: #1a7f37;
    }
    .diff-row-insert .diff-line-no {
      background: #ccffd8;
      color: #1a7f37;
    }
    .diff-row-equal td {
      background: #fff;
      color: #24292f;
    }
    .diff-cell-empty {
      background: #f6f8fa !important;
    }
    .diff-view-container {
      display: flex;
      flex-direction: column;
      height: 100%;
      overflow: hidden;
    }
    .diff-view-scroll {
      flex: 1;
      overflow-y: auto;
      overflow-x: auto;
    }
    .diff-no-changes {
      padding: 40px;
      text-align: center;
      color: #57606a;
      font-size: 14px;
    }
    #diffButton.active {
      color: #0969da;
      background: rgba(9, 105, 218, 0.08);
    }
```

- [ ] **Step 2: 确认构建无错误**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

---

### Task 5: 在 `main.ts` 中实现 diff 视图逻辑

**Files:**
- Modify: `src/client/main.ts`

这是核心任务，包含：
1. `diffViewActive` 状态变量
2. `handleDiffButtonClick()` — 触发 diff 视图
3. `renderDiffView()` — 渲染 diff 内容
4. `closeDiffView()` — 关闭 diff 视图
5. `acceptDiffUpdate()` — 接受更新
6. 修改 `updateToolbarButtons()` — 控制 diffButton 显示
7. 修改 `switchFile()` / `syncFileFromDisk()` — 切换文件时清理 diff 状态

- [ ] **Step 1: 在文件顶部 import 区添加 diff 工具导入**

找到 `main.ts` 顶部的 import 区（第1行附近），添加：

```ts
import { diffLines } from './utils/diff';
```

- [ ] **Step 2: 在 `fileRefreshSeq` 变量附近添加 diff 状态变量**

找到 `const fileRefreshSeq = new Map<string, number>();` 这行，在其后添加：

```ts
let diffViewActive = false;
```

- [ ] **Step 3: 添加 `renderDiffView` 函数**

在 `updateToolbarButtons` 函数之前添加：

```ts
// ==================== Diff 视图 ====================

async function loadPendingContent(path: string): Promise<string | null> {
  const file = state.sessionFiles.get(path);
  if (!file) return null;
  // 如果已缓存，直接返回
  if (file.pendingContent !== undefined) return file.pendingContent;
  // 否则从磁盘加载
  const data = await loadFile(path, true);
  if (!data) return null;
  file.pendingContent = data.content;
  return data.content;
}

function renderDiffView(oldContent: string, newContent: string): void {
  const container = document.getElementById('content');
  if (!container) return;

  const lines = diffLines(oldContent, newContent);
  const hasChanges = lines.some(l => l.type !== 'equal');

  if (!hasChanges) {
    container.innerHTML = `
      <div class="diff-view-container">
        <div class="diff-header">
          <div class="diff-header-titles">
            <div class="diff-header-old">← 当前版本</div>
            <div class="diff-header-new">磁盘最新版本 →</div>
          </div>
          <div class="diff-actions">
            <button class="diff-close-btn" onclick="window.closeDiffView()">关闭</button>
          </div>
        </div>
        <div class="diff-no-changes">文件内容与磁盘一致，无差异</div>
      </div>
    `;
    return;
  }

  // 构建 side-by-side diff 行
  // 将 delete/insert 配对，equal 直接输出
  const rows: Array<{ left?: typeof lines[0]; right?: typeof lines[0] }> = [];
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    if (line.type === 'equal') {
      rows.push({ left: line, right: line });
      i++;
    } else if (line.type === 'delete') {
      // 尝试与下一个 insert 配对
      if (i + 1 < lines.length && lines[i + 1].type === 'insert') {
        rows.push({ left: line, right: lines[i + 1] });
        i += 2;
      } else {
        rows.push({ left: line });
        i++;
      }
    } else {
      // insert without matching delete
      rows.push({ right: line });
      i++;
    }
  }

  const escHtml = (s: string) => s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');

  const tableRows = rows.map(({ left, right }) => {
    if (left && right && left.type === 'equal') {
      return `<tr class="diff-row-equal">
        <td class="diff-line-no">${left.oldLineNo}</td>
        <td>${escHtml(left.content)}</td>
        <td class="diff-line-no">${right.newLineNo}</td>
        <td>${escHtml(right.content)}</td>
      </tr>`;
    }
    const leftNo = left ? `<td class="diff-line-no">${left.oldLineNo ?? ''}</td>` : `<td class="diff-line-no diff-cell-empty"></td>`;
    const leftCell = left ? `<td class="diff-row-delete-cell">${escHtml(left.content)}</td>` : `<td class="diff-cell-empty"></td>`;
    const rightNo = right ? `<td class="diff-line-no">${right.newLineNo ?? ''}</td>` : `<td class="diff-line-no diff-cell-empty"></td>`;
    const rightCell = right ? `<td class="diff-row-insert-cell">${escHtml(right.content)}</td>` : `<td class="diff-cell-empty"></td>`;

    const rowClass = left && right ? 'diff-row-mixed' : left ? 'diff-row-delete' : 'diff-row-insert';
    return `<tr class="${rowClass}">
      ${leftNo}${leftCell}${rightNo}${rightCell}
    </tr>`;
  }).join('');

  container.innerHTML = `
    <div class="diff-view-container">
      <div class="diff-header">
        <div class="diff-header-titles">
          <div class="diff-header-old">← 当前版本</div>
          <div class="diff-header-new">磁盘最新版本 →</div>
        </div>
        <div class="diff-actions">
          <button class="diff-accept-btn" onclick="window.acceptDiffUpdate()">接受更新</button>
          <button class="diff-close-btn" onclick="window.closeDiffView()">关闭</button>
        </div>
      </div>
      <div class="diff-view-scroll">
        <div class="diff-view">
          <table class="diff-table">
            <colgroup>
              <col style="width:40px">
              <col style="width:calc(50% - 40px)">
              <col style="width:40px">
              <col style="width:calc(50% - 40px)">
            </colgroup>
            <tbody>${tableRows}</tbody>
          </table>
        </div>
      </div>
    </div>
  `;
}

async function handleDiffButtonClick(): Promise<void> {
  if (!state.currentFile) return;
  const file = state.sessionFiles.get(state.currentFile);
  if (!file) return;

  if (diffViewActive) {
    closeDiffView();
    return;
  }

  const newContent = await loadPendingContent(state.currentFile);
  if (newContent === null) return;

  diffViewActive = true;
  const diffBtn = document.getElementById('diffButton');
  if (diffBtn) diffBtn.classList.add('active');

  renderDiffView(file.content, newContent);
}

function closeDiffView(): void {
  diffViewActive = false;
  const diffBtn = document.getElementById('diffButton');
  if (diffBtn) diffBtn.classList.remove('active');
  renderContent();
}

async function acceptDiffUpdate(): Promise<void> {
  if (!state.currentFile) return;
  const file = state.sessionFiles.get(state.currentFile);
  if (!file || file.pendingContent === undefined) return;

  // 用 pendingContent 更新 content，清除 dirty 状态
  file.content = file.pendingContent;
  file.pendingContent = undefined;
  file.displayedModified = file.lastModified;
  saveState();

  diffViewActive = false;
  renderContent();
  syncAnnotationsForCurrentFile(false);
  flashContentUpdated();
  renderSidebar();
  await updateToolbarButtons();
}
```

- [ ] **Step 4: 修改 `updateToolbarButtons` 函数，控制 diffButton 显示**

将现有的 `updateToolbarButtons` 函数替换为：

```ts
async function updateToolbarButtons() {
  const diffButton = document.getElementById('diffButton');
  const refreshButton = document.getElementById('refreshButton');

  if (!state.currentFile) {
    if (diffButton) diffButton.style.display = 'none';
    if (refreshButton) refreshButton.style.display = 'none';
    return;
  }

  const file = state.sessionFiles.get(state.currentFile);
  if (!file) return;

  if (file.isMissing) {
    if (diffButton) diffButton.style.display = 'none';
    if (refreshButton) refreshButton.style.display = 'none';
    return;
  }

  const isDirty = file.lastModified > file.displayedModified;
  if (diffButton) diffButton.style.display = isDirty && !file.isRemote ? 'flex' : 'none';
  if (refreshButton) refreshButton.style.display = isDirty ? 'flex' : 'none';
}
```

- [ ] **Step 5: 修改 `switchFile` 函数，切换文件时清理 diff 状态**

找到 `switchFile` 函数，在函数开头（`removeSyncInfoPopover()` 之后，或第一行）添加：

```ts
  // 切换文件时关闭 diff 视图，清理 pendingContent
  if (diffViewActive) {
    diffViewActive = false;
    const diffBtn = document.getElementById('diffButton');
    if (diffBtn) diffBtn.classList.remove('active');
  }
```

- [ ] **Step 6: 修改 `syncFileFromDisk`，刷新时清理 pendingContent**

在 `syncFileFromDisk` 函数中，找到 `targetFile.content = data.content;` 这行，在其后添加：

```ts
  targetFile.pendingContent = undefined;  // 清理缓存的 pending 内容
```

同时，如果 diff 视图正在展示当前文件，刷新后关闭 diff 视图。在 `renderContent()` 调用之前添加：

```ts
  if (diffViewActive) {
    diffViewActive = false;
    const diffBtn = document.getElementById('diffButton');
    if (diffBtn) diffBtn.classList.remove('active');
  }
```

- [ ] **Step 7: 将函数挂载到 window**

在 `main.ts` 末尾的 `window.*` 赋值区域（搜索 `window.switchFile` 或 `window.removeFile`），添加：

```ts
(window as any).handleDiffButtonClick = handleDiffButtonClick;
(window as any).closeDiffView = closeDiffView;
(window as any).acceptDiffUpdate = acceptDiffUpdate;
```

- [ ] **Step 8: 确认构建无错误**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

Expected: `✅ Build completed!`

---

### Task 6: 修复 diff 行样式（mixed 行）

**Files:**
- Modify: `src/client/css.ts`

当一行既有删除又有插入（delete+insert 配对），需要左右两侧分别着色。在 diff 样式中补充：

- [ ] **Step 1: 在 diff CSS 中添加 mixed 行和单元格样式**

在 `src/client/css.ts` 的 diff 样式块中，在 `.diff-row-equal` 之后添加：

```css
    .diff-row-mixed .diff-row-delete-cell {
      background: #ffebe9;
      color: #b42318;
    }
    .diff-row-mixed .diff-line-no:first-child {
      background: #ffd6d1;
      color: #b42318;
    }
    .diff-row-mixed .diff-row-insert-cell {
      background: #e6ffec;
      color: #1a7f37;
    }
    .diff-row-mixed .diff-line-no:last-of-type {
      background: #ccffd8;
      color: #1a7f37;
    }
```

- [ ] **Step 2: 最终 build**

```bash
cd /Users/huanghao/workspace/md-viewer && just build-client 2>&1 | tail -5
```

Expected: `✅ Build completed!`

---

### Task 7: 手动测试和收尾

- [ ] **Step 1: 刷新浏览器，打开一个 markdown 文件**

- [ ] **Step 2: 在编辑器里修改该文件，保存**

Expected: tab 上出现 M 标记，工具栏出现 `[± Diff]` 和 `[↻ 刷新]` 按钮

- [ ] **Step 3: 点击 `[± Diff]`**

Expected: 内容区切换为 diff 视图，删除行红色，新增行绿色，`[± Diff]` 按钮高亮

- [ ] **Step 4: 点击「接受更新」**

Expected: 内容更新为最新版本，M 标记消失，diff 视图关闭，内容区有闪烁动画

- [ ] **Step 5: 再次修改文件，点击 `[± Diff]`，然后点「关闭」**

Expected: diff 视图关闭，内容区恢复原来的渲染，M 标记仍然存在

- [ ] **Step 6: 切换到另一个 tab，再切回来**

Expected: diff 视图已关闭（切换文件时自动清理）

- [ ] **Step 7: commit**

```bash
cd /Users/huanghao/workspace/md-viewer
git add src/client/utils/diff.ts src/client/types.ts src/client/html.ts src/client/css.ts src/client/main.ts src/client/embedded-client.ts
git commit -m "feat: 添加文件 diff 视图，M 标记时可对比当前版本与磁盘最新版本"
```
