# 待完成功能

1）我会不定期追加任务
2）每次完成其中一个，完成后在这条任务的下方标记完成状态、时间、简要描述，再增加一个分隔符
3）如果涉及到体验变化，比如命令行参数、交互、视觉等，或者复杂的策略变化，请先做设计方案，等我选择和确认后再继续。设计方案中以产品、流程设计为主，可以写API/接口设计，但不用贴具体代码（除非是非常核心的代码）

## 现在看代码量还要用这种字符串的方式来实现吗？
- 还是说对agent来说区别不大？
- IDE的语法检查会有辅助效果吗？

## 加一个用km-cli同步到学城的功能
- 需要一个parent-id作为输入，默认为最后添加的位置（类似于最近保存）
- 最近添加的几个位置也需要保留，可以在某个地方点击进行选择
- 也可以手动输入，id或者完整的url
- 同步完整以后，需要个小标记来提示已经同步过了

## 小红点
- 文件是新的，我没点开过

## 文件更新提示
- 小红点，或者其他工具类似的视觉效果，你告诉我，我判断一下
- 我们展示的内容和文件实际内容之间有差异
- 我们展示的内容和学城同步的内容之间有差异
- 因为现在有3个状态了，还是需要设计一下，一共会有多少种情况，每种情况下我们的状态展示是什么样的
- 但是学城上也可以编辑，而且我不想去同步学城编辑的状态，而且我们也没有覆盖学城文档的能力，所以同步需要一个别的方式，比如已经存在再同步就自动加v2？
先把设计文档整理出来，把交互也设计出来，我们看看

## tab太多怎么办
同样需要给我一些其他工具的设计方案，我来选择

## logo换一个
- 在浏览器的tab也上也很难看
- 这个页面自己左上角的图标也很难看
- 你推荐几个可能好的图标设计，资源网站，给我一些建议，然后替我选一选

## 复制成功不用模式框
- 把消息通知都做成统一的toast

## 在本地文件的旁边再加一个打开，打开附近的其他文件
比如现在打开了 some/dir/a.md 展示，我需要一个方法，能快速操作打开 some/dir/b.md，就是位置离他比较近的，或者父子目录里的其他md
先给我出一个交互可能是什么样的方案，我来看看

## cli改名：md-viewer-cli还是改成mdv，方便输入命令

## 普通（关闭了tab的）文件
干脆去掉这种状态和对应的操作，没有必要设计多增加一层状态，多一层理解
关闭就没了，添加就进来。

## 侧边栏的样式
- 顶上是搜索
- 当前打开文件的是一种透明的外围椭圆
- 普通（有打开tab的）背景调正常，不用特殊的蓝色背景
- 当前文档的完整地址，点进去可以复制path

## 输入新文件
- 现在的输入改一个地方，比如输入栏放到中间来，回车就添加

## 当前的选择文件位置里的父目录点了没有反应，要重新设计一下交互行为

## 复制文件名的按钮

## 设计统一的UI样式

# -------- 以下的任务不要实现，我再想想 -------

# 代码越来越大，写到一个文件里还合适吗？

# 评论功能，主要面向后续怎么给agent进行回答，以及agent怎么继续回答使用

## 设计一个在本地快速打开的动作

## 左侧列表交互优化

## 启动playwright进行手工测试
同时让agent记录测试过程，发现错误自动修复
监控动作看看和它实现的预期是否一致

kimi说不太现实

## 页面上输入文件路径的地方能不能给些输入提示
你想想其他工具在输入文件的控件上都是怎么实现的？给几个方案让我选